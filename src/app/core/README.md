# Core

Reserved for application-wide infrastructure. Add auth/session services and HTTP configuration here when a real backend is selected. No simulated authentication or HTTP layer is included. Feature data belongs in each feature's data-access folder.
