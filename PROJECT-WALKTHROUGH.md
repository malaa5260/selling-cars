# رحلة تشغيل Selling Cars — خطوة بخطوة

ده دفتر الشرح بتاعنا. هنبدأ من المستخدم وهو بيكتب الرابط، وكل ما نشرح ملف هنضيف دوره في الرحلة هنا.
الملف بيتكمل تدريجيًا؛ العناوين اللي لسه ما شرحناهاش هنسيبها لوقتها.

**وصلنا لفين؟** مرّينا على رحلة التشغيل والراوت والبحث والـstore، وأضفنا مرورًا سريعًا على الكارت والتفاصيل والمفضلة وباقي الصفحات. تفاصيل `changeDetection` لسه مؤجلة، والمرور السريع مش شرح لكل سطر.

## 1. المستخدم يكتب الرابط في المتصفح

مثلاً، وإحنا مشغّلين المشروع محليًا:

```text
http://localhost:4200/cars
```

المتصفح بيبعت طلب للسيرفر. في التطوير، السيرفر اللي شغّلناه بـ`npm start` بيرجع صفحة التطبيق.
في استضافة الإنتاج لازم السيرفر يبقى متظبط يرجّع صفحة التطبيق للروابط الداخلية زي `/cars` كمان.

دي رحلة أول فتح للموقع أو إعادة تحميله. التنقل جوّه التطبيق بعد ما يفتح له مسار مختلف هنوضحه تحت.

## 2. السيرفر يرجّع صفحة البداية: index.html

الملف: [src/index.html](src/index.html)

المتصفح بيقرا الـHTML ويبني منه الصفحة. في ملفنا فيه إعداد اللغة العربية واتجاه الكتابة، وعنوان الموقع، وجوّه الـbody العنصر ده:

```html
<app-root></app-root>
```

ده المكان اللي Angular هيعرض فيه الكومبوننت الرئيسي للتطبيق.
وجود العنصر لوحده مش بيشغّل Angular؛ لازم كود JavaScript يبدأ التطبيق.

## 3. المتصفح يحمّل ويشغّل JavaScript

إحنا بنكتب الكود بـTypeScript، لكن المتصفح بيشغّل JavaScript الناتج عنه.
أدوات البناء بتجهّز ملفات JavaScript وCSS وبتضيف مراجعها لصفحة الـHTML اللي بتتقدّم للمتصفح.

عشان كده مش لازم تلاقي سطر تحميل السكريبت مكتوب بإيدنا في ملف `src/index.html`.
والمتصفح مش بيشغّل `main.ts` نفسه مباشرة.

## 4. نقطة بداية الكود: main.ts

الملف: [src/main.ts](src/main.ts)

السطر الأساسي:

```ts
bootstrapApplication(App, appConfig)
  .catch((err) => console.error(err));
```

معناه: ابدأ تطبيق Angular، وخلي `App` هو الكومبوننت الرئيسي، واستخدم إعدادات `appConfig`.
ولو بدء التطبيق فشل، الجزء الأخير بيطبع الخطأ في الـconsole.

الملف بيستورد:

- `bootstrapApplication`: الدالة اللي بتبدأ التطبيق.
- `App`: الكومبوننت الرئيسي.
- `appConfig`: إعدادات وخدمات التطبيق.

**ملحوظة عن الإعدادات:** [app.config.ts](src/app/app.config.ts) بيسجّل إعدادات الراوتر باستخدام `provideRouter(routes)`، ومعاه مستمعين عامّين لأخطاء المتصفح. تفاصيل الراوتر هنشرحها في خطوة مستقلة.

## 5. Angular يعرض الكومبوننت الرئيسي: App

الملف: [src/app/app.ts](src/app/app.ts)

الأجزاء اللي بتوصل الرحلة ببعض:

```ts
selector: 'app-root',
imports: [Shell],
templateUrl: './app.html',
```

- `selector`: اسم العنصر اللي الكومبوننت بيستخدمه، وهو نفس `app-root` الموجود في صفحة البداية.
- `imports: [Shell]`: بيخلّي كومبوننت الشِل متاح للاستخدام جوّه قالب التطبيق. الاستيراد لوحده مش بيعرضه.
- `templateUrl`: بيحدد الملف اللي فيه قالب الكومبوننت.

Angular يبدأ `App` عند عنصر `app-root` ويعرض القالب بتاعه.

## 6. قالب App يطلب عرض Shell

الملف: [src/app/app.html](src/app/app.html)

محتواه الحالي:

```html
<app-shell />
```

هنا فعلًا بنستخدم الشِل. Angular عارفه من `imports` في الكومبوننت، فبيعرضه داخل التطبيق.

## 7. وصلنا لـShell: الإطار العام للموقع

الملف: [src/app/layout/shell.ts](src/app/layout/shell.ts)

دوره العام:

- يعرض اسم المشروع وقائمة التنقل فوق.
- يوفّر مساحة في المنتصف لعرض الصفحة المطلوبة.
- يعرض التذييل تحت.

مكان عرض الصفحة موجود في القالب:

```html
<router-outlet />
```

الراوتر هو اللي بيحدد الكومبوننت المناسب للرابط وبيعرضه في المكان ده.
الشِل بيحتوي المكان؛ مش هو اللي بيقرر بنفسه أي صفحة تظهر.

### 7.1 أول سطرين: استيراد الأدوات

```ts
import { ChangeDetectionStrategy, Component } from '@angular/core';
import { RouterLink, RouterLinkActive, RouterOutlet } from '@angular/router';
```

صيغة `import { ... } from '...'` معناها: هات الأدوات اللي بين الأقواس من المكتبة المكتوبة بعد `from`، عشان نستخدمها في الملف.

السطر الأول بيجيب أدوات من أساس Angular:

- `Component`: بنستخدمها لتعريف الكلاس على إنه كومبوننت وتحديد إعداداته وقالب عرضه.
- `ChangeDetectionStrategy`: بنختار منها طريقة متابعة تغييرات العرض. الملف بيستخدم `OnPush`؛ هنشرح معناها عند سطر استخدامها.

السطر التاني بيجيب أدوات التنقل:

- `RouterLink`: يخلي الرابط ينقل المستخدم لصفحة داخل التطبيق.
- `RouterLinkActive`: يضيف كلاسات للرابط لما مساره يبقى نشط، عشان نميّز الصفحة الحالية.
- `RouterOutlet`: المكان اللي الراوتر بيعرض عنده كومبوننت الصفحة المطلوبة.

سطر الاستيراد لوحده مش بيرسم حاجة على الشاشة. هو بيخلّي الأدوات متاحة في كود الملف؛ بعده هنشوف تسجيل أدوات القالب واستخدامها.

### 7.2 الـselector: اسم العنصر اللي بنستخدمه لعرض الشِل

داخل إعدادات `@Component` في ملف الشِل بنكتب:

```ts
selector: 'app-shell',
```

ده بيقول لـAngular: لما تلاقي عنصر اسمه `app-shell` في قالب كومبوننت متاح له استخدام `Shell`، اعرض كومبوننت الشِل عند العنصر ده.

وعشان كده في `app.html` كتبنا:

```html
<app-shell />
```

الربط هنا بين اسم العنصر في القالب وقيمة `selector`. اسم الملف `shell.ts` واسم الكلاس `Shell` مش هما اللي بنكتبهم كتاج في القالب؛ بنكتب قيمة الـselector.

استخدامه هنا شغال لأن `App` بيضيف `Shell` في قائمة `imports` عنده. كتابة التاج لوحدها من غير إتاحة الكومبوننت للقالب مش كفاية.

نوع الـselector هنا هو **Element selector** لأننا بنستخدمه كاسم عنصر: `<app-shell />`.

### 7.3 قائمة imports والـstandalone

```ts
imports: [RouterLink, RouterLinkActive, RouterOutlet],
```

دي بتتيح أدوات التنقل لقالب الشِل: الروابط، وتمييز الرابط النشط، ومكان عرض الصفحة. الاستيراد أعلى الملف يتيح الأسماء لكود TypeScript؛ قائمة `imports` داخل إعدادات الكومبوننت تتيح الأدوات للقالب.

من **Angular 19**، حذف خاصية `standalone` معناه إن الكومبوننت standalone افتراضيًا. قبل Angular 19 كان لازم نكتب `standalone: true` عشان نعرّفه كـstandalone. ولو هنعرّف كومبوننت غير standalone في إصدار حديث بنكتب `standalone: false` ونصرّح بيه في NgModule.

الشِل في مشروعنا standalone لأننا بنستخدم Angular 22 ولم نغيّر الافتراضي. قائمة `imports` مسموحة للكومبوننت الـstandalone؛ وجودها مش هو اللي بيحوّله إلى standalone.

المصدر: [توثيق Angular — Anatomy of components](https://v20.angular.dev/guide/components).

### 7.4 قالب الشِل والكلاس

القالب مكتوب مباشرة داخل `template`، وفيه بالترتيب:

- رابط «انتقل للمحتوى»: ينقل المستخدم لمنطقة المحتوى داخل نفس الصفحة، ويساعد استخدام الكيبورد.
- `header`: اسم المشروع وقائمة الروابط. كلاسات Tailwind مسؤولة عن الشكل والتجاوب.
- روابط `routerLink`: تطلب الانتقال لمسار داخل التطبيق. `routerLinkActive` يضيف كلاسات تميّز الرابط النشط، و`ariaCurrentWhenActive` يوضح حالته لتقنيات المساعدة.
- `main` وبداخله `router-outlet`: مكان عرض كومبوننت الصفحة المختارة.
- `footer`: التنويه إن البيانات تجريبية.

بعد إعدادات `@Component` بنلاقي `export class Shell {}`. الكلاس فاضي حاليًا: مفيش فيه بيانات أو دوال خاصة بالشِل؛ القالب وأدوات الراوتر كفاية لدوره الحالي. `export` بيتيح استيراد الكلاس في ملف تاني، زي `app.ts`.

مرّينا على القالب والكلاس بشكل عام. شرح `changeDetection: ChangeDetectionStrategy.OnPush` لسه مؤجل.

## 8. من لينك في القائمة إلى صفحة داخل الشِل

هنمشي في الشرح برحلة فعلية: المستخدم بيعمل إيه، أي ملف يستقبل الخطوة، وبعده أي ملف يكمّلها، وإيه اللي يتغير على الشاشة.

الملف: [src/app/app.routes.ts](src/app/app.routes.ts)

ده جدول ربط المسارات بالصفحات. بيتسجّل عند بداية التطبيق عن طريق `provideRouter(routes)` في `app.config.ts`؛ مش بنشغّل ملف الإعدادات من جديد مع كل ضغطة.

| المسار | النتيجة الحالية |
| --- | --- |
| `/` | إعادة توجيه إلى `/cars` |
| `/cars` | فتح مسارات ميزة العربيات، ثم صفحة البحث |
| `/cars/:id` | تفاصيل العربية حسب المعرّف |
| `/favorites` | صفحة المفضلة |
| `/sell-car` | صفحة مبدئية لبيع العربية |
| `/my-listings` | صفحة مبدئية لإدارة الإعلانات |
| `/account` | صفحة مبدئية للحساب |
| أي مسار غير مطابق | صفحة «الصفحة مش موجودة» |

مثال: الضغط على رابط المفضلة في الشِل يطلب المسار `/favorites`. الراوتر يطابقه مع الإعداد الموجود في الجدول، ويحمّل كومبوننت `Favorites` عند الحاجة، ثم يعرضه عند `router-outlet`. الإطار العام للشِل يفضل موجود.

### 8.1 أول فتح للموقع: من المسار الفاضي للعربيات

في `app.routes.ts` أول إعداد هو:

```ts
{ path: '', pathMatch: 'full', redirectTo: 'cars' },
```

- `path: ''`: المستخدم فتح عنوان الموقع من غير مسار بعده.
- `pathMatch: 'full'`: إعادة التوجيه تحصل بس لما المسار كله يكون فاضي؛ مش لمجرد إن بدايته تطابق المسار الفاضي.
- `redirectTo: 'cars'`: بدل عرض صفحة هنا، وجّه المستخدم لمسار العربيات.

بعد إعادة التوجيه، الراوتر يطابق الإعداد التالي:

```ts
{ path: 'cars', loadChildren: () => import('./features/cars/cars.routes').then(m => m.CARS_ROUTES) },
```

`loadChildren` هنا بيحمّل **مجموعة مسارات** ميزة العربيات عند الحاجة، مش NgModule. `import()` تحميل ديناميكي للكود، وبعد التحميل بنختار منه القيمة المصدّرة `CARS_ROUTES`.

في `cars.routes.ts`، المسار الداخلي الفاضي يختار صفحة البحث:

```ts
{ path: '', title: 'العربيات | Selling Cars', loadComponent: () => import('./search/car-search').then(m => m.CarSearch) },
```

المسار الفاضي هنا معناه مفيش جزء إضافي بعد `/cars`، مش عنوان الموقع الرئيسي. `loadComponent` بيحمّل كومبوننت البحث نفسه، و`title` بيحدد عنوان تبويب المتصفح. الراوتر يعرض صفحة البحث عند الـoutlet الموجود في الشِل.

```text
فتح /
  → إعادة توجيه إلى /cars
  → تحميل CARS_ROUTES
  → مطابقة المسار الداخلي الفاضي
  → تحميل CarSearch وعرضه داخل الشِل
```

### 8.2 ليه اخترنا تنظيم الراوت بالشكل ده؟

هدف المشروع أساس لمنصة بيع عربيات بتنظيم حسب الميزة، قابل للتوسّع لما نضيف backend. فصل المسارات هنا قرار تنظيم للمشروع، مش قاعدة إن أي مشروع Angular لازم يبقى بنفس التقسيم.

- **ملف المسارات الرئيسي خريطة للمميزات:** يوضح مداخل العربيات والمفضلة والحساب والبيع، من غير تفاصيل كل ميزة.
- **العربيات ليها ملف مسارات خاص:** لأنها فيها صفحتين مرتبطتين بنفس المجال: البحث والتفاصيل. جمعهم في `cars.routes.ts` بيخلّي إضافة مسارات تخص العربيات بعدين في مكان واضح.
- **`loadChildren` للعربيات:** هنا بيرجع مصفوفة مسارات، مش NgModule. تنظيم المسارات منفصل عن استخدام NgModules.
- **`loadComponent` للصفحات المفردة:** الحساب والبيع وإعلاناتي والمفضلة كل واحدة حاليًا صفحة واحدة؛ مش محتاجين ملف مسارات إضافي لكل واحدة دلوقتي. وصفحتا البحث والتفاصيل نفسهم بيتحمّلوا بالطريقة دي داخل مسارات العربيات.
- **التحميل عند الحاجة:** بنؤجل كود الصفحة لحد ما يحتاجه المستخدم، وده يساعد نقلّل كود البداية. المقابل إن أول زيارة ممكن تحتاج طلب تحميل إضافي؛ مش وعد إن كل تنقل هيكون أسرع.
- **الشِل خارج الصفحات المتغيرة:** قائمة التنقل والتذييل مش بيتكرروا في كل صفحة؛ الراوتر يغيّر المحتوى عند الـoutlet المشترك.
- **المسار الرئيسي يوجّه للعربيات:** لأن البحث والتصفّح هما نقطة البداية الحالية للمنصة، بدل إنشاء صفحة رئيسية إضافية بنفس المحتوى.
- **مسار `**` آخر القائمة:** عشان يلتقط الروابط اللي ما طابقتش أي مسار معروف ويعرض صفحة مفهومة للمستخدم.

المسارات بتحدد التنقل والتحميل، لكنها مش نظام حماية. تسجيل الدخول والصلاحيات لسه مش مطبّقين في النسخة الحالية.

## 9. صفحة البحث: مصدر البيانات وحالة البحث

الملف: [car-search.ts](src/app/features/cars/search/car-search.ts)

الراوتر وصلنا للكومبوننت `CarSearch`. هنبدأ بمنطق الكلاس عشان نفهم البيانات اللي القالب بيعرضها.

```ts
protected readonly store = inject(CarsStore);
protected readonly query = signal('');
```

`inject(CarsStore)` بيطلب من نظام حقن الاعتماديات في Angular خدمة بيانات العربيات. الخدمة متوفرة على مستوى التطبيق باستخدام `providedIn: 'root'`، فصفحات البحث والتفاصيل والمفضلة بتستخدم نفس الحالة المشتركة في الإعداد الحالي. مش بنعمل نسخة بيانات مستقلة لكل صفحة.

حاليًا الخدمة فيها ثلاث عربيات تجريبية في الذاكرة؛ مفيش طلب API. فصلناها عن صفحة البحث لأن البيانات والمفضلة محتاجين يتشاركوا بين صفحات، بينما نص البحث يخص صفحة البحث نفسها.

`query` signal بتحتفظ بنص البحث، وبتبدأ بنص فاضي. `query()` تقرأ القيمة، و`query.set(...)` تغيّرها.

```ts
protected readonly filteredCars = computed(() => {
  const query = this.query().trim().toLocaleLowerCase();
  return this.store.cars().filter(car => `${car.make} ${car.model} ${car.city}`.toLocaleLowerCase().includes(query));
});
```

`filteredCars` نتيجة مشتقة من نص البحث والعربيات. بتشيل المسافات من أطراف البحث وتوحّد حالة الحروف، وتختار العربيات اللي الماركة أو الموديل أو المدينة فيها النص المطلوب. النص الفاضي يطابق كل العربيات.

لما المستخدم يكتب، حدث `input` يحدّث `query`، والنتيجة المشتقة تتحدّث عند قراءتها بناءً على القيمة الجديدة، والقالب يعرض النتائج. مفيش طلب للسيرفر مع الكتابة في النسخة الحالية.

### 9.1 يعني إيه CarsStore؟

الملف: [cars-store.ts](src/app/features/cars/data-access/cars-store.ts)

ده كلاس خدمة إحنا كاتبينه للمشروع، مش مكتبة جاهزة ولا قاعدة بيانات ولا NgRx. سمّيناه Store لأنه بيحتفظ بحالة العربيات والمفضلة وبيوفر طرق لقراءتها وتغييرها.

```ts
@Injectable({ providedIn: 'root' })
export class CarsStore { /* ... */ }
```

`@Injectable` يعرّف الكلاس كخدمة قابلة للحقن، و`providedIn: 'root'` يوفّرها على مستوى التطبيق. الصفحات في إعدادنا الحالي بتطلب نفس نسخة الخدمة باستخدام `inject(CarsStore)`.

محتوياته الحالية:

- `inventory`: signal خاصة بتخزّن قائمة العربيات التجريبية.
- `cars`: واجهة قراءة للـsignal باستخدام `asReadonly()`، فلا تستطيع الصفحة استدعاء `set` أو `update` عليها. ده مش تجميد عميق للكائنات.
- `favoriteIds`: signal خاصة بمعرّفات العربيات اللي المستخدم اختارها للمفضلة.
- `favorites`: قائمة مشتقة من العربيات ومعرّفات المفضلة.
- `isFavorite`: بتقول هل العربية في المفضلة.
- `toggleFavorite`: تضيف المعرّف للمفضلة أو تشيله لو موجود.

مثال: تضيف عربية للمفضلة من صفحة البحث، وبعدين تفتح صفحة المفضلة؛ تلاقيها هناك لأن الصفحتين بيستخدموا نفس الخدمة. البيانات في ذاكرة التطبيق فقط؛ إعادة تحميل الصفحة بتمسح اختيارات المفضلة الحالية.

فصلناه عن الكومبوننت عشان الكومبوننت يهتم بعرض الصفحة والتفاعل، والخدمة تحتفظ بالحالة المشتركة وقواعد تغييرها. شرحنا هنا دوره ومكوّناته العامة؛ ممكن نتتبّع كل سطر عند استخدامه.

### 9.2 ربط حقل البحث وعرض النتائج

داخل قالب `CarSearch`، حقل البحث فيه:

```html
[value]="query()"
(input)="query.set($any($event.target).value)"
```

`[value]` يقرأ نص البحث من الـsignal ويحطه في الحقل. `(input)` يستقبل حدث تغيير محتوى الحقل أثناء الكتابة، ويكتب النص الجديد في الـsignal. `$event.target` هو عنصر الإدخال هنا، و`$any` بيتجاوز فحص نوعه في القالب للوصول إلى `value`؛ مش بيحوّل القيمة وقت التشغيل. دول ربط قيمة واستماع لحدث بشكل صريح، مش استخدام `ngModel`.

بعد تغيّر النص، `filteredCars` يشتق النتائج المناسبة، والقالب يقرأها في مكانين: عدد النتائج، وحلقة العرض:

```html
@for (car of filteredCars(); track car.id) {
  <app-car-card
    [car]="car"
    [favorite]="store.isFavorite(car.id)"
    (favoriteToggle)="store.toggleFavorite($event)"
  />
} @empty {
  <!-- رسالة مفيش نتائج وزر مسح البحث -->
}
```

`@for` يعرض كارت لكل عربية مطابقة. `track car.id` يعطي Angular هوية ثابتة لكل عربية، عشان يربط عناصر العرض بالعربيات ويعيد استخدامها لما القائمة تتغيّر.

صفحة البحث تبعت بيانات العربية للكارت عن طريق `[car]`، وحالة المفضلة عن طريق `[favorite]`. الكارت يبلّغ الصفحة بطلب تغيير المفضلة من خلال حدث `favoriteToggle`؛ قيمة `$event` هنا معرّف العربية، مش حدث إدخال من المتصفح. الصفحة تطلب من الخدمة تنفيذ التغيير.

`@empty` يعرض رسالة لو القائمة فاضية. زر مسح البحث ينفّذ `query.set('')`، فيفضى الحقل وترجع كل العربيات التجريبية.

**نقطة التوقف:** شرحنا ربط حقل البحث بالـsignal، وعدد النتائج، وحلقة عرض الكروت، وحالة عدم وجود نتائج. الخطوة الجاية كومبوننت `CarCard` وكيف يستقبل البيانات ويصدر حدث المفضلة.

## 10. مرور سريع على بقية المشروع

### كارت العربية

الملف: [car-card.ts](src/app/features/cars/ui/car-card.ts)

`CarCard` كومبوننت عرض قابل لإعادة الاستخدام. يستقبل العربية في `input.required<Car>()` وحالة المفضلة في `input(false)`. يعرض الاسم والسنة والمسافة والسعر؛ `DecimalPipe` المستخدم كـ`number` ينسّق الأرقام.

عند الضغط على المفضلة يرسل معرّف العربية عبر `favoriteToggle.emit(car().id)`، والصفحة الأم تطلب التغيير من الخدمة. الكارت لا يحقن الـstore، وده بيسمح باستخدام نفس الكارت في البحث والمفضلة. الضغط على اسم العربية ينتقل إلى `/cars/` ومعه المعرّف.

### تفاصيل العربية

الملف: [car-details.ts](src/app/features/cars/details/car-details.ts)

المسار `:id` يستقبل المعرّف. `ActivatedRoute.paramMap` يوفر معاملات المسار، و`toSignal` يتيح قراءتها كـsignal. `computed` يبحث في قائمة العربيات عن المعرّف الحالي؛ لو المعرّف اتغير أثناء استخدام نفس الكومبوننت، النتيجة تتحدّث. `@if` يعرض التفاصيل لو العربية موجودة، وإلا يظهر تنبيه إن العربية مش موجودة.

### المفضلة

الملف: [favorites.ts](src/app/features/favorites/favorites.ts)

تقرأ `store.favorites()`، وهي قائمة مشتقة من بيانات العربيات ومعرّفات المفضلة. تستخدم نفس `CarCard`. إزالة عربية تحدّث الخدمة فتخرج من القائمة، ولو القائمة فضيت تظهر رسالة بدل الكروت. لا يوجد حفظ دائم حاليًا.

### الصفحات والأجزاء الأخرى

- `sell-car` و`my-listings` و`account`: صفحات مبدئية توضّح المميزات اللي لسه محتاجة backend؛ لا يوجد نشر إعلان أو تسجيل دخول فعلي.
- `shared/ui/empty-state.ts`: عرض عام لعنوان ووصف ومحتوى إضافي باستخدام `ng-content`، بدون منطق خاص بالعربيات.
- `layout/not-found.ts`: صفحة للرابط غير المعروف؛ تختلف عن حالة معرّف عربية غير موجود داخل مسار التفاصيل الصحيح.
- `core`: مكان محجوز للبنية المشتركة مستقبلًا، زي المصادقة وإعداد HTTP عند الحاجة.
- `car.ts`: نوع TypeScript يصف بيانات العربية، مش مصدر البيانات نفسه.
- Tailwind: التنسيق في كلاسات القوالب؛ ملف CSS يحتوي استيراد Tailwind وتفعيله يتم عبر PostCSS.
- `app.spec.ts`: اختبارات لتصفية البحث ومشاركة المفضلة، وتغيير معرّف التفاصيل، والروابط والحالات غير الموجودة. الـbuild والاختبارات الأربعة نجحوا عند تجهيز النسخة.

**نقطة التوقف الحالية:** اكتمل مرور سريع على أجزاء النسخة الحالية. لم نربط backend، ولم نشرح كل سطر أو تفاصيل OnPush؛ يمكن التعمّق في هذه الأجزاء لاحقًا.

## الرحلة لحد هنا

```text
المستخدم يكتب الرابط
  → السيرفر يرجّع index.html
  → المتصفح يقرا الصفحة ويحمّل JavaScript
  → كود main.ts يبدأ التطبيق
  → Angular يعرض App عند app-root
  → قالب App يستخدم app-shell
  → Shell يعرض الإطار العام ومكان الصفحة
```

## فرق مهم: أول تحميل والتنقل الداخلي

في أول تحميل أو عند عمل Refresh، المتصفح بيطلب الصفحة من السيرفر وبيبدأ التطبيق.
بعد ما التطبيق يشتغل، الضغط العادي على رابط داخلي باستخدام `routerLink` بيخلّي Angular Router يغيّر الصفحة داخل `router-outlet`، من غير إعادة تحميل مستند `index.html` بالكامل.
ممكن يحمّل كود الصفحة عند الحاجة لو كانت lazy-loaded.

## الخطوات اللي هنضيف شرحها بعدين

- تفاصيل ملف الشِل.
- إعدادات التطبيق وملفات المسارات: إزاي الرابط يحدد الصفحة؟
- تحميل صفحة البحث عند فتح مسار العربيات.
- مصدر البيانات التجريبية.
- إزاي signals وcomputed بيحدّثوا البحث والمفضلة؟
- رحلة فتح تفاصيل عربية والرجوع للقائمة.

مع كل شرح جديد هنضيف الخطوة في مكانها ونحدّث نقطة التوقف، عشان الملف يفضل ماشي معانا بنفس ترتيب الفهم.
